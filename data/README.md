# Data Directory

This directory is reserved for generated project data.

- `processed/`: Task 02 will write parsed JSON outputs here.
- `app.db`: future SQLite database file, ignored by git.

Do not modify files in `示范景区公开资料包/`; they are read-only source materials.
